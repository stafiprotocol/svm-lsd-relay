# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.DS_Store

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt

# IDE
.idea/
.vscode/
.history

# Builds
build/
bin/
site/

# golang
.env
keys/

# Chainbridge
config.toml
config*.json
*.block

centrifuge-chain/
/cfgBuilder/*.toml
reth_log*.json

# Dependency directories (remove the comment below to include it)
# vendor/
log_file/
log_data/
conf_dev.*
conf_main.*
config_start.toml
config_init.toml
config_set.toml
config_transfer_admin.toml
config_build_upgrade_tx.toml
config_transfer_token.toml
config_create_token_metadata.toml
config_next.toml
config_init_staking_pool.toml
config_init_stake_manager.toml